#include<stdio.h>
int main(){
// 01
    // char name[] = "Mani Kumar";
    // printf("Your name is: %s", name);

// 02
    // char name[50];
    // printf("Enter the name: ");
    // scanf("%s", name); // Only the frst word will be considered..

    // name[0] = 'R';
    // printf("your name is: %s", name);

// Ex
    // char str[20];
    // printf("Enter the name: ");
    // gets(str);
    // puts(str);

    return 0;
}
