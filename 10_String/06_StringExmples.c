#include<stdio.h>
#include<string.h>
int main(){
// 01: strlen(name)
    // char name[] = "sandeep";
    // printf("Length is: %d", strlen(name));

// 02: strcpy(newStr, oldStr)
    // char oldStr[] = "oldStr";
    // char newStr[] = "newStr";

    // strcpy(newStr, oldStr);
    // puts(newStr);

// 03: strcat(firstStr, secStr)
    // char firstStr[100] = "Mani ";
    // char secStr[] = "Kumar ";
    // char lastStr[] = "Sah";

    // strcat(secStr,firstStr);
    // strcat(firstStr, lastStr);
    // puts(secStr);

// 04: strcmp(firstStr, senSte)
    // char firstStr[] = "Apple";
    // char secStr[] = "Banana";

    // printf("%d", strcmp(firstStr, secStr));

 // Ex   
    // char firstStr[] = "Ajvchjgdc";
    // char secStr[] = "Bsssd";
    
    // printf("%d", strcmp(firstStr, secStr));

    
// Ex 01: 
    // char firstStr[] = "Apple";
    // char secStr[] = "Banana";

    // printf("%d", strcmp(secStr, firstStr));  

// Exam 02: 
    // char firstStr[] = "Apple";
    // char secStr[] = "Apple";

    // printf("%d", strcmp(firstStr, secStr));

/*Write a function to insert a new character in 
a string at a given position.*/
    // char str[20] = "College";
    // for (int i=6; i>=2; i--){
    //     str[i+1] = str[i];
    // }
    // str[2] = 'k';
    // printf("%s",str);

    return 0;
}

