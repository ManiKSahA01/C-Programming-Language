# include<stdio.h>
int main(){
// Scaneer
    // int age;
    // printf("Enter age: ");
    // scanf("%d", & age);
    
    // printf("age is: %d", age);

    
// Input (Sum of 2 numbers)
    float a, b, sum;
    printf("Enter the two number:\n");
    scanf("%f %f", &a, &b);

    sum = a + b;
    printf("Sum is: %g",sum);

    return 0;
}

